from pura.services import LocalDatabase, create_tables
import asyncio
import pytest


# def test_create_tables():
#     loop = asyncio.new_event_loop()
#     asyncio.set_event_loop(loop)

#     loop.run_until_complete(create_tables("pura.db"))
